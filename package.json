{
  "name": "@ashaffer/vdux-local",
  "description": "Bind a vdux component to local state using redux-ephemeral",
  "repository": "git://github.com/ashaffer/vdux-local.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "rm -rf lib && babel src --out-dir lib",
    "postpublish": "rm -rf lib"
  },
  "peerDependencies": {
    "redux-ephemeral": "*"
  },
  "dependencies": {},
  "devDependencies": {
    "babel": "^5.8.21",
    "babel-tape-runner": "^1.2.0",
    "standard": "^5.1.0",
    "tape": "^4.2.0"
  }
}
